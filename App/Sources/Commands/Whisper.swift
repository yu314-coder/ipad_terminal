import Foundation

enum Whisper {
    static func run(args: [String]) -> String {
        // Placeholder: wire your whisper.cpp wrapper here.
        // Expected flags: <audioPath> --model <ggml> [--gpu]
        return "whisper: stub (wire whisper.cpp iOS/Metal build and parse args)"
    }
}
