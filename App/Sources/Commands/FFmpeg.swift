import Foundation

enum FFmpeg {
    static func run(args: [String]) -> String {
        // Placeholder: call into FFmpegKit's execute() with the joined args
        // Example: FFmpegKit.execute("-i \(in) -ar 16000 \(out)")
        return "ffmpeg: stub (wire FFmpegKit execute() here)"
    }
}
